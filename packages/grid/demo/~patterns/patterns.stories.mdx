import { Meta, Canvas, Story } from '@storybook/addon-docs';
import { PaneProvider, Pane } from '@zendeskgarden/react-grid';
import { SplitterCardStory } from './stories/SplitterCardStory';
import { CARD_SPLITTER_COLUMNS } from './stories/data';

<Meta title="Packages/Grid/[patterns]" />

# Patterns

## Card-based Splitter Layout

<Canvas>
  <Story
    name="Card-based Splitter"
    args={{
      columns: CARD_SPLITTER_COLUMNS
    }}
    argTypes={{
      columns: { name: 'children', table: { category: 'Story' } }
    }}
    parameters={{
      design: {
        allowFullscreen: true,
        type: 'figma',
        url: ''
      }
    }}
  >
    {args => <SplitterCardStory {...args} />}
  </Story>
</Canvas>
